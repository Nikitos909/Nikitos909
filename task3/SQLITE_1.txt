SELECT CustomerId, FirstName, Phone, Company
FROM Customer AS c
WHERE EXISTS(SELECT E.EmployeeId
                 FROM Employee AS E
                 WHERE e.EmployeeId = c.SupportRepId
                   AND BirthDate >= DATE('now', '-50 years'))
				   ORDER by City ASC, Email DESC


Пока ничего не понял

